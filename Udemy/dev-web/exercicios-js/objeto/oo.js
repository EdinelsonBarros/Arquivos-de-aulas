// CÓDIGO NÃO EXECUTÁVEL!!!

//PROCEDURAL
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        //...
    }
}

objeto.processamento() // o foco passou a ser objeto e não a função. Na linguagem OO temos objetos que tem funções, enquanto na procedural temos funções que manipula dados.

// Princípios importantes:

// 1. abstraçao -> trazer caracteristicas importantes para o projeto, do mundo real para o código.

// 2. encapsulamenton -> deixar os objetos encapsulados de forma que eles não interfiram uns nos outros mais do que o necessários.

//3. Herança -> fazer obejtos maiores apartir de objetos menores. Priorize composição em vez da herança.

//4. Poliformismo ->